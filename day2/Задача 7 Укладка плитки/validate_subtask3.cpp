#include "testlib.h"

using namespace std;

int main()
{
    registerValidation();
    
    int n = inf.readInt(1, 100000);
    inf.readSpace();
    int k = inf.readInt(0, 0);
    inf.readEoln();
    inf.readEof();

    return 0;
}
#include <cmath>
#include <iostream>
#include <io.h>
#include <stdio.h>
using namespace std;
int main () {
    int a, k, b, m, x;
    freopen(stdin, stdout, "forest.in");
    scanf('%d %d %d %d %d', &a, &k, &b, &m, &x)
    l = floor((x*k*m)/(a*m*(k-1)+b*k*(m-1)))
}
